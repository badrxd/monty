#include "monty.h"
/**
 * stack_f - prints the top
 * @head: the stack head
 * @counter: line_number
 * Return: no return
*/
void stack_f(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	var.lifi = 0;
}
